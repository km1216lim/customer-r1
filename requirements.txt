# Core training
torch>=2.4.0
transformers>=4.45.0
accelerate>=1.0.0
deepspeed>=0.15.0
flash-attn>=2.6.0

# RL framework (pick one; verl is the primary path)
verl>=0.3.0
# openrlhf>=0.5.0  # alternative

# Rollout
vllm>=0.6.3
ray[default]>=2.35.0

# Data
datasets>=3.0.0
pyarrow>=17.0.0
huggingface_hub>=0.25.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
jinja2>=3.1.0
PyYAML>=6.0.0

# Rationale synthesis (Vertex AI Gemini 2.5 Pro; service-account auth via google-auth)
google-genai>=0.3.0
google-auth>=2.34.0
python-dotenv>=1.0.0
# Legacy / optional rationale-synth providers (kept for ad-hoc comparisons)
anthropic>=0.40.0
openai>=1.50.0

# Logging
wandb>=0.18.0
tqdm>=4.66.0
numpy>=1.26.0

# Eval (rationale quality, optional but used by --rationale_metrics)
bert-score>=0.3.13
rouge-score>=0.1.2

# Tests
pytest>=8.0.0
